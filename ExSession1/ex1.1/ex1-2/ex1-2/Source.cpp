#include <stdio.h>

int main()
{
	/*once an integer is added to the printing process, %d needs to be added, 
	in order for the compiler to recongize that what is next to be printed is an integer*/
	int c = 5;
	printf("The integer is %d\n", c);

	while (1);
	return 0;
}